# Refuse to install/run on a Node version that doesn't satisfy `engines.node` (>=24).
# Without this, a wrong-Node install silently builds a mismatched better-sqlite3
# native binary and the DB test suite fails confusingly at run time instead of here.
engine-strict=true
