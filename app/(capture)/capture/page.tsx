// Mobile capture-only surface (UX-DR8). Deliberately non-responsive: no Board,
// Claim Cards, Proof Brief, or Report here. The paste-link / screenshot / note
// intake path is built in Epic 2 (Story 2.5). Phase 0: placeholder only.
// Route group `(capture)` keeps files separate; the URL is `/capture`.
export default function CaptureHome() {
  return (
    <main>
      <h1>ProofDesk Capture</h1>
      <p>Mobile capture-only path — scaffold only. Evidence intake arrives in Epic 2.</p>
    </main>
  );
}
