// src/core — PURE audit engine (AD-1, AD-16). Decision table + Specification
// predicates + structured trace. Imports NOTHING effectful: no Next.js, no React,
// no Node effect modules, no Date.now(), no DB/network/file access. It never
// re-classifies a value the shell already resolved. Filled in Story 1.5.
export {};
