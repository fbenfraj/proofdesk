// src/export — self-contained HTML report, print CSS (3-channel status), ZIP +
// CSV/JSON manifests, trust footer (AD-12, AD-20, AD-22). Uses fflate (ZIP) and
// csv-stringify. Filled in Epic 4. Empty seam in Phase 0.
export {};
