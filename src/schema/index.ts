// src/schema — Drizzle schema + Zod validators, sharing the glossary vocabulary
// (AD-3). Home of the versioned AuditSnapshot contract type (AD-16) — the seam
// everything crosses. Defined in Story 1.3. Empty seam in Phase 0.
export {};
